# Anthropic API
ANTHROPIC_API_KEY=sk-ant-...

# ntfy.sh topic — make this unguessable, e.g. waiver-wire-kd7x92
# Anyone who knows this string can subscribe, so treat it like a password
NTFY_TOPIC=waiver-wire-abc123

# Set automatically by render.yaml — do not add to Render env vars
ENV=staging
